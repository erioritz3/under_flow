#include <iostream>
#include <iomanip>
#include <string>
#include <fstream>
#include <cstdlib>
using namespace std;

// this program calculates the total rain fall for the year
//const int WIDTH = 8;
//const int DEC_P = 1;
const int SIZE = 2;// size or # of elements in array

string month[SIZE]={"Jan","Feb"};
double total[SIZE];
double average[SIZE];

// function prototypes
//double getRainfall(int station_id); // input validation for getting temperature
int main()

{
    
    cout << "Enter the total number rainfall for: \n";
    
    for(int count = 0; count < SIZE; count++)
    {
        output_parallel_array(month,total);
        cout << month[count] << endl;
        cin >> total[count];
    }
    return 0;
}
